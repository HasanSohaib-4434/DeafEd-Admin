<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Admin Home</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" rel="stylesheet">
  <style>
    body {
      background-color: #e3f2fd; /* Light Blue */
    }

    .container {
      max-width: 800px;
      margin: 50px auto;
      padding: 20px;
      background-color: #ffffff; /* White */
      border-radius: 10px;
      box-shadow: 0 4px 8px rgba(0, 0, 0, 0.2);
    }

    h3 {
      color: #1565c0; /* Dark Blue */
    }

    .btn-danger {
      background-color: #e53935; /* Red */
      border-color: #e53935;
    }

    .profile p {
      font-size: 18px;
      color: #424242; /* Dark Gray */
    }

    /* Card styling */
    .card {
      margin: 15px;
      cursor: pointer;
      transition: transform 0.3s ease-in-out;
    }

    .card:hover {
      transform: translateY(-10px);
    }

    .card-body {
      text-align: center;
      color: #1565c0;
    }

    .card-title {
      font-size: 1.5rem;
      font-weight: bold;
    }

    .card-text {
      font-size: 1.2rem;
    }

  </style>
</head>

<body>
  <div class="container">
    <h3 class="text-center">Welcome, <%= user.fullname %></h3>
    <div class="profile">
      <p>Username: <%= user.username %></p>
      <p>Email: <%= user.email %></p>
    </div>

    <div class="row mt-4">
      <div class="col-md-3">
        <div class="card" onclick="window.location.href='/manage-student'">
          <div class="card-body">
            <h5 class="card-title">Manage Student</h5>
          </div>
        </div>
      </div>
      <div class="col-md-3">
        <div class="card" onclick="window.location.href='/manage-educator'">
          <div class="card-body">
            <h5 class="card-title">Manage Educator</h5>
          </div>
        </div>
      </div>
      <div class="col-md-3">
        <div class="card" onclick="window.location.href='/manage-section'">
          <div class="card-body">
            <h5 class="card-title">Manage Section</h5>
          </div>
        </div>
      </div>
      <div class="col-md-3">
        <div class="card" onclick="window.location.href='/manage-class'">
          <div class="card-body">
            <h5 class="card-title">Manage Class</h5>
            
          </div>
        </div>
      </div>
    </div>

    <div class="text-center mt-4">
      <button class="btn btn-danger" id="backButton">Logout</button>
    </div>
  </div>

  <script>
    document.getElementById('backButton').addEventListener('click', () => {
      if (confirm('Do you want to logout?')) {
        window.location.href = '/logout';
      }
    });
  </script>
</body>

</html>
